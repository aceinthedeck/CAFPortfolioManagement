var strMsg; //Variable declaration, don't have to declare type

function ShowMsg(){
	//show the contents of #par1
	//dom is the document object model and has to be referred to
	alert(document.getElementById("par1").innerHTML);
	//The same code above written in jquery would be just $("#par1")
	
}
